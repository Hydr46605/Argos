/**
 * Marker annotations expressing API stability across the library surface.
 *
 * They are informational (no compile-time enforcement): undocumented wire
 * shapes can drift regardless of annotations, but the markers give consumers
 * an at-a-glance risk gauge and give IDEs the usual tooltips.
 */
@file:Suppress("unused")

package it.hydr4.argo.annotations
