/**
 * Runnable, well-commented usage samples.
 *
 * These classes compile into the library jar but are never invoked by the
 * library itself; they exist so the README quickstart and the integration
 * scripts share one source of truth.
 */
@file:Suppress("unused")

package it.hydr4.argo.examples
