# Pins the Gradle daemon to a JDK 17 toolchain, keeping every local task —
# including detekt's embedded Kotlin compiler, which predates JDK 25 — on a
# single deterministic JVM. Resolved from installed JDKs via auto-detection.
toolchainVersion=17
